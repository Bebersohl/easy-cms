

extends layout
include ./mixins.pug
block content
  h3 Update a #{modelName}
  if errors
    +errorList(errors)
  form(method="post")
    +fields(fields)

    button(type='submit') Update
    button Cancel
  pre.
    [[fields]]
    !{JSON.stringify(fields, null, 2)}
    
    [[schema]]
    !{JSON.stringify(schema, null, 2)}